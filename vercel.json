{
  "buildCommand": "env | grep -E '^(DATABASE|NEXTAUTH|CALENDSO|NEXT_PUBLIC|SKIP_)' > .env && npx turbo run build --filter=@calcom/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "yarn install",
  "framework": "nextjs"
}
